extends base.pug
include mixins/video

block content
    nav
    ul
        li
            a(href="/videos/upload") Upload
    //mixin part
    each video in videos
        +video(video)
    else
        li Sorry nothing found!